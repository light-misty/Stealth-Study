# 2026-09-08

## 龙外工作台（HIU WorkSpace）PRD v1.0

- 产出：`docs/PRD-龙外工作台.md`（1588 行 / 约 12.7 万字节 / 11 张 Mermaid 图 / 16 章）
- 由 software-company 团队产品经理许清楚主笔，交付总监齐活林编排；工作流判定为「仅 PRD」部分工作流。

### 用户已拍板的四个前提（后续开发不要推翻）
1. 首发形态：桌面端为主，沿用 Tauri + 本地 Python 服务，本地优先。
2. 暂不对接校方系统（教务/学工/一卡通/图书馆/统一身份认证），数据一律文件手动导入。
3. 模型资源：师生自带 API Key（BYOK），沿用现有多提供商接入。
4. 文档深度：完整版（含竞品分析 7 款、Mermaid 象限图、市场定位与推广路径）。

### 代码核实结论（重要，影响架构决策）
- `coworker/providers/registry.py` 已实现 deepseek / kimi / qwen / zai(GLM) / minimax / xai / mistral / ollama / ark 等，BYOK 无需新增适配，属「复用」。
- `stt/src/lib.rs` 默认模型为 `ggml-base.en.bin`（英语专用），11 语种必须换多语种模型，是 M2 前置改造项。
- `coworker/personas/manifest.py` 的 `VALID_GROUPS` 仅 `{"general","security"}`；校园人设建议不改上游枚举，改用 `hiu-student-*` / `hiu-teacher-*` id 前缀区分，避免上游同步冲突。
- 内置 16 个人设全部安全/DevOps 向，与校园场景无关，校园人设为纯新增目录。
- `coworker/tools/subagent.py` 的 `explore` 是只读子 Agent，不能直接做带写权限的批量批改（教师端 P0），编排方式待定。

### 待用户/校方拍板的阻断级问题
Q-01 试点期模型费用谁出 / Q-02 校内对接人与决策路径 / Q-03 校方是否已有 AI 使用管理办法 / Q-04 首批试点院系与种子用户 / Q-05 教师端是否允许导入处理学生个人信息（影响批量批改设计）。

## PRD v2.0 修订（同日完成，覆写同一文件）

用户答复后触发范围变更，PRD 升级 v2.0（1571 行 / 11 图 / 19 条变更明细 C-01~C-19）：
- **面向用户收窄为「只有学生」单角色**：删除全部教师端（教案、命题、批量批改、评分量规）与依赖校方制度的管理端功能；功能规划重构为「公共基础 + 学生场景」两层；P0 由 25 项压缩到 12 项。
- **学生群体扩展为三类**：A 外语类 / B 计算机与信息技术类 / C 其他非外语类，各有独立画像与 journey 图。
- **定位变更**：非校方项目，是学生个人课程作业；无校内对接人、校内无 AI 管理办法、严格 BYOK 学生自付 Key、试点面向任何普通学生。
- 推广改为同学自发传播，商业模式改为「本项目不商业化」。
- 里程碑重排为 A 档 8 周 / B 档 12 周（推荐）/ C 档 16 周三档可裁剪方案。
- AI 生成内容标识判定为 P1（保留最简显式标识 + 导出声明），隐式水印不做；但「AI 使用留痕/审计导出」留在 P0。

### 代码核实的重要纠正（推翻了先前假设）
- `coworker/personas/builtin/` 下 16 个内置人设中 **13 个标了 `ships: false`**（含 swe-worker / test-worker / change-worker / design-worker），**不随发布版分发**，需 `OPENWORKER_UNSHIPPED=1` 才可见。默认只分发 security / cloud-posture / dep-audit 三个（均为安全向）。
- 且这些人设是 `team: worker` 类型，系统提示明确「对 LEAD 说话、绝不使用 ask_user」，学生单机会话直接用会交互错乱。**工程向人设不可直接复用**。
- 计算机场景真正可复用的主资产是 **`coworker/agents/code.py`**：完整的 solo 编码代理（`CODE_CAPABILITIES = ["code_files","git","search","shell","todo"]`，63 行系统提示含探索优先、最小改动、改完必跑测试、不主动 commit、不记录密钥）。暴露为人设即可，几乎零代码。
- 代码库理解场景可复用 `coworker/tools/subagent.py` 的 `explore` 只读研究子 Agent。

### 工期估算（产品经理估算，未经开发侧复核）
P0 共 32 人日 + 必需非 P0 开销 14 人日 ≈ **46 人日 ≈ 368 小时**。按 20h/周需 18.4 周（超一学期），25h/周 14.7 周，30h/周 12.3 周，40h/周 9.2 周。

### 阻断级待确认（用户尚未答复）
Q-01 作业截止日期 / Q-02 课程评分标准侧重 / Q-03 是否需真实可运行原型 / Q-04 单人每周可投入工时。

## PRD v3.0 修订（同日完成，覆写同一文件）

用户要求：去掉作业属性，当作正常产品项目；团队开发 + AI Coding Agent 工具。PRD 升 v3.0（1616 行 / 16 章 / 11 图 / 变更明细 C-20~C-28），已扫描确认「人日 / 每周工时 / 一学期 / 截止日期 / 答辩 / 课程作业」零命中。

- **里程碑改为迭代制 I1-I7**：I1 基座与人设体系 → I2 计算机场景 / I3 外语场景（文本）/ I4 通识与数据导入（三者可在 I1 后并行）→ I5 语音与口语陪练（依赖 I1+I3）→ I6 打包真机验证与内测 → I7 扩展沉淀（可选）。时间盒只给建议值，工作量用 XS/S/M/L，不绑定人数与周期。
- **P0 由 12 项增至 13 项**：表格与 Office 导入解析（B-07）由 P1 升 P0。
- **优先级重判**：口语陪练 / STT 多语种改造由"最先裁"改为"P0 且不可裁剪"（实测校准是新瓶颈，不能因为难就砍掉最差异化部分）；错词本错题库 P2→P1；考级备考维持 P1（瓶颈是外语专业学生验证资源，非产能）；AI 标识维持 P1（合规判断，与产能无关）；课表导入维持 P2。
- 新增 10.6「AI Coding Agent 对二开策略的结构性影响」：模式清晰可枚举的工作边际成本大幅下降，瓶颈转移到"调通链路 + 实测校准 + 真机验证"；排期原则第 3 条为"优先做已存在能力的暴露与串联，不重写；能力清点是 AI 编码效率的前提"。
- 风险重写 14 条，新增 R-03 AI 生成代码一致性与可维护性、R-04 跨成员分支冲突、R-06 人设技能缺乏真实语料校准、R-08 四层链路联调（Tauri ↔ Rust sidecar ↔ Python ↔ 模型 API）。
- 待确认问题重做为 21 个，新增 Q-15 团队分支策略与代码评审机制（关系 R-03/R-04，建议开发启动前定）。

### PRD v3.0 定稿状态（已复核）

1637 行 / 16 章 / 11 个 Mermaid 图 / emoji 0 / 禁词（人日、每周工时、一学期、截止日期、答辩、课程作业）0 命中。

**文档编号约定（后续写设计/任务书要遵守）**：全文档以**功能编号**（B-xx 公共基础 / C1-xx 计算机 / C2-xx 外语 / C3-xx 通识）为准；P0-xx 只是第 5.4 节 P0 清单的行号，两者不可混用。5.4 末尾有「P0 编号 ↔ 功能编号」完整对照表。第 11 章各迭代范围已统一改为功能编号。

### 龙外校情（来源 hiu.edu.cn，检索日 2026-09-08，官网口径存在打架，已并列呈现）
黑龙江省唯一语言类本科高校；11 个外语语种；全日制本科在校生 11709 人（另有 10136 / 8822 口径）；专任教师 523 人；外籍教师 28 人；10 个二级学院；智慧教室 198 间。专业数 32 vs 34 存疑。
关键利好：翻译专业已设「智能翻译」方向、英语专业已设「数智创新班」，2026-08 承办全国「人工智能赋能翻译教学研修班」——校方对 AI+外语已有明确意愿。

## 定位重构需求（用户新指令，方案已提交待拍板）

- 15:10 提交 `3239f34 doc: 删除龙外工作台PRD文档`，工作区已无 PRD 文件（v3.0 内容仍在 git 历史 62a498d / d688231 中可恢复）。
- 新需求：产品定位重构为**通用大学生学习类应用**（四六级、考研等），去院系/校名绑定；**明确区分学生端与教师端**差异化需求；强调"不要添加过多功能，以项目已有 AI 功能为主"。
- 与既有前提的冲突（待用户拍板）：①「只有学生、无教师端」被推翻，教师端深度待定；② 龙外校情/院系绑定内容是否整体移除；③ 里程碑 A/B/C 三档与 I1-I7 是否重排。
- 拟执行顺序（本轮已向用户提出）：架构师先做「已有能力盘点 + 可行性矩阵（复用/改造/新增 三档）」→ 产品经理在能力清单上做映射写 PRD → 架构师做技术校验与迭代拆分 → 交付总监做口径一致性审查。QA 不进场。
- 已扫到的可复用资产（粗查，深度待架构师核实）：personas 人设体系、skills 技能包、memory(SQLite) 长期记忆、automation(cron) 定时任务、tools(files/directories/search/shell/git/plan/todo/subagent/ask)、teams(board/journal)、permissions + AuditView 审计、STT(Rust whisper) 语音输入、GUI 已有 PersonasTab/MemorySection/SkillsTab/ScheduledView/Composer.voice/PlanCard/TodoPanel/FolderGate。

## 定位重构已交付（完整 PRD v1.0）

- 团队 `software-study-buddy` 完成：架构师能力盘点 + 产品经理完整 PRD + 主理人口径一致性审查（首跑 architect 因 API 限流 429 失败，已轻量重启 architect-2 成功）。
- 产出：`docs/capability-audit-2026-09-08.md`（高见远；A 档约 9 / B 档约 4 / C 档约 3；**核心缺口 = 写文件工具缺失**，files.py 仅 read_file）+ `docs/PRD-学伴AI.md`（许清楚；352 行 / 13 章 / 3 图 / 10 项 P0）。
- 产品名：**学伴 AI（StudyBuddy）**，去校名化重构；`HIU-WorkSpace` / `coworker` 仅作仓库内部代号。
- 定位：通用大学生 AI 备考学习桌面应用，仅学生端（教师端仅文档描述不实现），三备考台（四六级/考研/证书），核心闭环 = 向 AI 取资料 → 一键沉淀本地 → 检索复习。
- 用户最终拍板（覆盖旧前提）：① 不做教师端；② 首期三台 = 四六级+考研+证书（不含日常课业/编程课设）；③ 核心机制 = AI 资料存本地+本地复习；④ 重新设计品牌去校名化。
- 关键结论：C 档仅三项（write_file、STT 多语种/中文模型、本地资料检索/复习视图）即首期功能边界；三台差异由人设+技能包+计划模板配置解决，技术底座共用。
- 阻断级待确认：R1 STT 模型选型与分发、R2 新人设纳入 release、R3 本地 Python sidecar 启动与首启引导。
- 注：AGENTS.md 项目指南仍写「只有学生单一角色、无教师端」（与本次一致），但其"龙外专属"定位未同步为"通用大学生"，后续可据用户确认再同步文档。

## PRD v1.1 修订（用户三项拍板，同日落盘）

- 语音：**本项目不做任何语音功能**，关闭并隐藏现有语音入口（B-04 改写为「语音功能边界：不做并隐藏」，P0）；删听力/复试口语项；R1 解除。
- 人设：改为「AI 老师」向（用户是学生）；**弃用 hiu 前缀**，改用 `xueban-*`（xueban-cet / kaoyan / cert）；落盘目录 `.hiu/notes/` 改 `.xueban/notes/`。
- 安装即用：用户确认现有打包已实现安装即用与首启配置 API Key，沿用即可，无首启引导需求；R3 解除、R4 降级为低。
- 迭代收敛 I1-I5 → I1-I4；唯一剩余阻断级待确认 = R2 人设分发机制（ships:true+group:general vs 改构建脚本）。
- 工程备注：PRD 编辑期间出现「Edit 报成功但内容被回写覆盖」的混合状态（疑似预览进程占用，EBUSY 为先兆）；最终以整文件 Write 重写 v1.1 终稿解决。后续编辑该文件若再现 EBUSY 或校验不通过，优先整文件重写并 grep 验证。

## 学伴 AI 设计文档阶段（仅提交不推送）

- 架构师高见远产出 `docs/design-学伴AI-架构与任务分解.md`（DESIGN-StudyBuddy-v1.0，348 行）：8 任务 T01-T08 映射 I1-I4；关键决策 D1-D6（write_file 独立模块不改 files.py、memories 表扩 5 列承载知识卡片、Leitner 盒零依赖、语音 features.ts 特性开关隐藏、三备考台差异全在配置层、xueban-* 用 group:general 不改 VALID_GROUPS）。零新增依赖。待明确 9 条，阻断级 1 条：xueban-* 人设进 release 机制（R2 遗留）。
- QA 严过关产出 `docs/test-plan-学伴AI.md`（TP-StudyBuddy-v1.0，325 行）：P0 用例 59 条 + 非功能 5 条，pytest/Vitest/Playwright 三层；B-04 语音隐藏 6 条负向、B-05 落盘 10 条、B-11 人设护栏；PRD 第 8 章 10 条验收标准全映射；待确认 8 条（TQ-01~08）。
- 本地提交链（未推送）：37f348f（PRD v1.2）→ 4f7b9dc（测试计划）→ b980eb6（架构设计）。
- 引用竞态再次出现两次（4f7b9dc、b980eb6 提交后 HEAD 未前进），均以 Python 直写 loose ref + git pack-refs --all 修复；注意 refs/heads/doc 目录可能不存在，需先 makedirs。

## 用户授权团队自行裁决 + 文档重组（仅提交不推送）

- 用户授权：xueban-* 人设分发机制与架构 9 条待明确由开发团队按最佳方案自行决定。架构师逐条裁决并写入 `docs/3-架构设计-学伴AI.md` 第 8 章决策记录。关键结论：1) ships:true+group:general 直接分发，零构建脚本改动；5) 不声明 requires_folder；6) 打卡默认 cron 0 21 * * *；7) B-12 首期不启用；8) 内容评审走团队内部（PM 初稿+架构/QA 复核）；9) I4 尝试一行排除 stt，改不动则保留。
- 文档按 1-6 编号重组：1-能力盘点 / 2-PRD / 3-架构设计 / 4-任务分解 / 5-测试计划 / 6-人设与技能包（PM 许清楚新写，CONTENT-StudyBuddy-v1.0，367 行）；旧 design-学伴AI-架构与任务分解.md 已 git rm。
- 本地提交链追加：8d1cac7（重命名）→ 4c53c20（协作者）→ 89bbb1f（6- 新增）→ 81e16bb（拆分+决策）→ f4639be（6- 对齐决策5）。全部未推送。
- 严重事件：并发会话把整个 docs/ 目录从工作区删除（未暂存），且一次提交竞态中 `1db5ae4` 记录了 6- 整文件删除（成为悬挂提交，未入链）。恢复方法：`git reset` + `git checkout HEAD -- docs` 从 HEAD 完整还原。引用竞态本轮共出现 5 次（8d1cac7/89bbb1f/81e16bb/f4639be 等），均以 Python 直写 loose ref + pack-refs 修复。
