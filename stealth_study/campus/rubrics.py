"""`rubrics.py` — rubric / 归因定义的**唯一权威常量**（05 §4.2、05 §5）。

每条常量都是纯文本模板字符串（不含任何代码逻辑），头部注释写明它逐字对应哪个技能包
SKILL.md 的哪个章节。师资服务链路（人设会话的技能包）与 REST 批改链路（`grading.py`）
共用同一份文本，由 `tests/campus/test_rubrics_consistency.py` 的一致性测试锁定（08 §5.1）。

SKILL.md 要到 T21 才落盘，本阶段一致性测试以 05 文档原文为锁定目标；落盘后测试会自动
改为对 SKILL.md 逐字比对（见测试文件注释）。

这里的辅助常量（`.ESSAY_BANDS` / `ESSAY_BAND_INTERVALS` / `ERROR_TYPES_CET` 等）同样不携带
任何逻辑，只作为 `grading.py` 校验器与 `service.py`（CET-12 TOP3 聚合键）引用的白名单。
"""

from __future__ import annotations

# 与 `personas/builtin/cet-grader/skills/cet-essay-grading/SKILL.md` 的
# 「四六级短文写作评分标准」章节保持逐字一致，改动必须双向同步。
CET_ESSAY_RUBRIC = """# 四六级短文写作评分标准（15 分制，官方五档）

评分流程：通读定档（下表）→ 按内容/结构/语言三维各 5 分拆分展示 → 三维之和必须落在该档
分值区间内 → 逐条列错误 → 升格示范一段。

| 档位 | 分值 | 判据（官方表述） |
|---|---|---|
| 一档 | 14 分 | 切题。表达思想清楚，文字通顺、连贯，基本上无语言错误，仅有个别小错。 |
| 二档 | 11 分 | 切题。表达思想清楚，文字连贯，但有少量语言错误。 |
| 三档 | 8 分 | 基本切题。有些地方表达思想不够清楚，文字勉强连贯；语言错误相当多，其中有一些是严重错误。 |
| 四档 | 5 分 | 基本切题。表达思想不清楚，连贯性差。有较多的严重语言错误。 |
| 五档 | 2 分 | 条理不清，思路紊乱，语言支离破碎或大部分句子均有错误，且多数为严重错误。 |
| 零分 | 0 分 | 未作答，或只有几个孤立的词，或文不对题。 |

分项维度（各 0-5 分，用于批改卡展示）：内容（切题度与要点覆盖）、结构（段落组织与衔接手段）、
语言（用词与语法的准确性、句式多样性）。换算：15 分制原始分 × 7.1 = 106.5 分制得分。

错误类型枚举（供错误清单使用，与 rubrics.py 的 ERROR_TYPES 一致）：主谓一致 / 时态语态 / 动词
搭配 / 冠词与单复数 / 中式英语 / 逻辑连接缺失 / 用词不当 / 拼写。

批改纪律：先定档后拆分，禁止"分项之和与档位矛盾"；每条错误给原文片段+修改建议+类型；
升格示范必须基于学生原句改写，不得换题重写。输出标注"AI 生成，仅供参考"。"""

# 与 `personas/builtin/cet-grader/skills/cet-translation-grading/SKILL.md` 的
# 「四六级段落翻译评分标准」章节保持逐字一致，改动必须双向同步。
CET_TRANSLATION_RUBRIC = """# 四六级段落翻译评分标准（15 分制，官方六档）

| 档位 | 分值 | 判据（官方表述） |
|---|---|---|
| 一档 | 13–15 | 译文准确表达了原文的意思。用词贴切，行文流畅，基本上无语言错误，仅有个别小错。 |
| 二档 | 10–12 | 译文基本上表达了原文的意思。文字通顺、连贯，无重大语言错误。 |
| 三档 | 7–9 | 译文勉强表达了原文的意思。用词欠准确，语言错误相当多，其中有些是严重语言错误。 |
| 四档 | 4–6 | 译文仅表达了一小部分原文的意思。用词不准确，有相当多的严重语言错误。 |
| 五档 | 1–3 | 译文支离破碎。但除个别词语外，未表达原文意思。 |
| 零分 | 0 | 未作答，或只有几个孤立的词，或译文与原文毫不相关。 |

逐句批改法：把原文按意群切句，逐句给"原文 → 学生译文 → 参考译文 → 问题类型"；问题类型枚举：
漏译 / 误译 / 时态 / 单复数 / 冠词 / 词性误用 / 搭配不当 / 语序 / 中式英语。整段档位由逐句质量
加权得出（三分之一以上句子漏译或误译即落入四档以下）。换算：15 分制 × 7.1 = 106.5 分制。
输出标注"AI 生成，仅供参考"。"""

# 与 `personas/builtin/study-companion/skills/mistake-attribution/SKILL.md` 的
# 「错题归因五分类」章节保持逐字一致，改动必须双向同步。
ATTRIBUTION_TAXONOMY = """# 错题归因五分类（固定枚举，与 rubrics.py 的 ATTRIBUTION_TAXONOMY 一致）

| 归因 | 判定标准（满足即归此类） |
|---|---|
| 概念不清 | 对知识点本身理解错误或缺失——换一种问法仍会错 |
| 审题失误 | 知识点会，但看错/漏看题目条件、答非所问 |
| 计算或操作失误 | 思路对，执行环节出错（算错、操作步骤错、抄错） |
| 超纲或不熟 | 考纲边缘内容或首次接触的题型，谈不上掌握 |
| 时间不够 | 会做但没做完/被迫乱选——正确率在限时外可复现 |

输出固定格式：`建议归因：<类别>｜依据：<一句话，引用题目中的具体证据>｜置信：<高/中/低>`。
规则：① 五类互斥，选最主导的一个；② 证据必须是题目原文或学生作答中的具体内容，不得空泛；
③ 置信为"低"时明确建议"待确认"，让用户在错题本中手动选择；④ 你只输出建议——最终归类由
用户确认后落库，不要声称"已记录"。"""

# 与 `personas/builtin/cert-instructor` 人设正文（05 §3.5）所述的「主观题评分点三态输出契约」
# 保持一致，供 `grading.py` 的 `kind=short_answer/essay_material/lesson_plan/practical` 分支使用。
CERT_SCORING_POINTS_SPEC = """# 主观题评分点三态输出契约

每个得分点给出"命中 / 部分命中 / 未命中 + 说明 + 对应知识点"；未命中的得分点由应用自动
降级关联知识点掌握度。输出一个 JSON 对象：

{"scoring_points": [{"point": "<评分点>", "status": "hit|partial|miss", "note": "<作答证据>", "point_ref": "<对应知识点>"}],
 "overall_score": <0-总分>, "upgraded_demo": "...", "model_answer_outline": "..."}

三态定义：hit=完整命中（作答包含该得分点全部要素）；partial=部分命中（仅含部分要素）；
miss=未命中（作答未覆盖或答错）。每条评分点必须填 note（引用作答原文中的具体证据）与
point_ref（映射到的知识点名）。输出标注"AI 生成，仅供参考"。"""

# 作文错误类型白名单（供 CET-12 TOP3 聚合键，单独导出）。
ERROR_TYPES_CET: tuple[str, ...] = (
    "主谓一致",
    "时态语态",
    "动词搭配",
    "冠词与单复数",
    "中式英语",
    "逻辑连接缺失",
    "用词不当",
    "拼写",
)

# 翻译错误类型白名单（05 §4.4 逐句批改法的问题类型枚举）。
TRANSLATION_ERROR_TYPES: tuple[str, ...] = (
    "漏译",
    "误译",
    "时态",
    "单复数",
    "冠词",
    "词性误用",
    "搭配不当",
    "语序",
    "中式英语",
)

# 作文 15 分制档位与档位区间（06 §2.2 校验器的白名单与反推依据）。
ESSAY_BANDS: tuple[int, ...] = (14, 11, 8, 5, 2, 0)
ESSAY_BAND_INTERVALS: dict[int, tuple[int, int]] = {
    14: (13, 15),
    11: (10, 12),
    8: (7, 9),
    5: (4, 6),
    2: (1, 3),
    0: (0, 0),
}
ESSAY_DIMENSIONS: tuple[str, ...] = ("content", "structure", "language")

# 翻译为连续 0-15 档。
TRANSLATION_BAND_MIN = 0
TRANSLATION_BAND_MAX = 15

# 主观题得分点三态（CERT_SCORING_POINTS_SPEC 的输出契约）。
SCORING_POINT_STATUSES: tuple[str, ...] = ("hit", "partial", "miss")